{
  "name": "agent-foundry",
  "private": true,
  "type": "module",
  "version": "0.2.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:core": "tsc -p tsconfig.build.json",
    "lint": "echo 'lint pending'",
    "test": "echo 'test pending'",
    "demo": "npm run build:core && node dist/services/orchestrator/src/demo.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
